const express = require('express');
const database = require('../src/database');
const Ticket = require('../src/Ticket');

function partitionierteTickets(linie) {
    const router = express.Router();




    return router;
}

module.exports = partitionierteTickets;
